services:
  {% if backend %}
  backend:
    build:
      context: ./{{ module_name }}-backend
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "8080:8080"
  {% endif %}
  {% if frontend %}
  frontend:
    build:
      context: ./{{ module_name }}-frontend
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
  {% endif %}
  {% if job %}
  job:
    build:
      context: ./{{ module_name }}-job
      dockerfile: Dockerfile
    env_file:
      - ./.env
  {% endif %}