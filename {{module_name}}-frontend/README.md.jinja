# {{ project_name }} frontend

Vue 3 + Vite + TypeScript starter.

## Start

```bash
npm install
npm run dev
```

> **First-time setup:** After applying this template, commit the generated
> `package-lock.json` before running a Docker build (`npm ci` requires it).
> This is analogous to committing `uv.lock` for the Python backend/job.